{
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "npm@9.6.7",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "turbo run build --filter=@anton-ui/docs && changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "turbo": "^2.5.8"
  }
}
